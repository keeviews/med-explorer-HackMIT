# DiscussMeds web UI

React + TypeScript + Vite frontend. Run it from the **repository root** instructions in the root `README.md`.

```bash
npm install
npm run dev
```

Dev server: http://127.0.0.1:41789  
API (set in `.env`): `VITE_API_URL=http://127.0.0.1:18765`
